---
description: Prepare a deep company research prompt/report
argument-hint: "[company|URL]"
---
Use the project skill `.pi/skills/career-ops/SKILL.md`.

Treat this invocation as:

`/career-ops deep $ARGUMENTS`

Load the required context files for `deep` mode and execute the workflow.
