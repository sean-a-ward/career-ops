---
description: Flag overdue follow-ups and draft messages
argument-hint: "[filters]"
---
Use the project skill `.pi/skills/career-ops/SKILL.md`.

Treat this invocation as:

`/career-ops followup $ARGUMENTS`

Load the required context files for `followup` mode and execute the workflow.
