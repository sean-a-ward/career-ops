---
description: Scan configured portals for new offers
argument-hint: "[filters]"
---
Use the project skill `.pi/skills/career-ops/SKILL.md`.

Treat this invocation as:

`/career-ops scan $ARGUMENTS`

Load the required context files for `scan` mode and execute the workflow.
