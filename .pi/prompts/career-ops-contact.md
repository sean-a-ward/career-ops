---
description: Find LinkedIn contacts and draft outreach
argument-hint: "[company|role]"
---
Use the project skill `.pi/skills/career-ops/SKILL.md`.

Treat this invocation as:

`/career-ops contacto $ARGUMENTS`

Load the required context files for `contacto` mode and execute the workflow.
