---
description: Assist with an application form; never submit
argument-hint: "[URL]"
---
Use the project skill `.pi/skills/career-ops/SKILL.md`.

Treat this invocation as:

`/career-ops apply $ARGUMENTS`

Load the required context files for `apply` mode and execute the workflow.
