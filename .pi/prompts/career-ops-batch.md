---
description: Batch process multiple offers with pi workers
argument-hint: "[options]"
---
Use the project skill `.pi/skills/career-ops/SKILL.md`.

Treat this invocation as:

`/career-ops batch $ARGUMENTS`

Load the required context files for `batch` mode and execute the workflow.
