---
description: Show application tracker status
argument-hint: "[filters]"
---
Use the project skill `.pi/skills/career-ops/SKILL.md`.

Treat this invocation as:

`/career-ops tracker $ARGUMENTS`

Load the required context files for `tracker` mode and execute the workflow.
