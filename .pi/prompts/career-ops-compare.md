---
description: Compare and rank multiple offers
argument-hint: "[reports|criteria]"
---
Use the project skill `.pi/skills/career-ops/SKILL.md`.

Treat this invocation as:

`/career-ops ofertas $ARGUMENTS`

Load the required context files for `ofertas` mode and execute the workflow.
