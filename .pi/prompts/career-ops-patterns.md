---
description: Analyze rejection patterns and improve targeting
argument-hint: "[filters]"
---
Use the project skill `.pi/skills/career-ops/SKILL.md`.

Treat this invocation as:

`/career-ops patterns $ARGUMENTS`

Load the required context files for `patterns` mode and execute the workflow.
