---
description: Process pending job URLs from data/pipeline.md
argument-hint: "[limit|filters]"
---
Use the project skill `.pi/skills/career-ops/SKILL.md`.

Treat this invocation as:

`/career-ops pipeline $ARGUMENTS`

Load the required context files for `pipeline` mode and execute the workflow.
