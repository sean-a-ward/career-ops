---
description: Evaluate a portfolio project idea
argument-hint: "[idea]"
---
Use the project skill `.pi/skills/career-ops/SKILL.md`.

Treat this invocation as:

`/career-ops project $ARGUMENTS`

Load the required context files for `project` mode and execute the workflow.
