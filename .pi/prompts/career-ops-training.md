---
description: Evaluate a course or certification against goals
argument-hint: "[course]"
---
Use the project skill `.pi/skills/career-ops/SKILL.md`.

Treat this invocation as:

`/career-ops training $ARGUMENTS`

Load the required context files for `training` mode and execute the workflow.
